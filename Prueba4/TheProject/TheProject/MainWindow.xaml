<Window x:Class="TheProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TheProject"
        xmlns:view="clr-namespace:TheProject.ViewModels"
        xmlns:models="clr-namespace:TheProject.Models"
        mc:Ignorable="d"
        
        
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        xmlns:faWPF="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"

        WindowStartupLocation="CenterScreen"
        Title="Window2" Height="700" Width="1300">
    <Border CornerRadius="0" Name="thescreen">

        <Border BorderThickness="2" 
             CornerRadius="0">

            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{StaticResource winBorderColor1}" Offset="0" />
                    <GradientStop Color="{StaticResource winBorderColor2}" Offset="0.75" />
                    <!--<GradientStop Color="{StaticResource winBorderColor3}" Offset="1" />-->
                </LinearGradientBrush>
            </Border.BorderBrush>

            <Border.Background>
                <LinearGradientBrush StartPoint="0,1" EndPoint="1,0" >

                    <GradientStop Color="{StaticResource primaryBackColor1}" Offset="0.55" />

                </LinearGradientBrush>
            </Border.Background>



            <Grid Name="Grid_1">

                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="250" />

                    <ColumnDefinition Width="*" />

                </Grid.ColumnDefinitions>




                <Grid Grid.Column="2" Name="G_Gear">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="100" />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" >

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Ellipse >
                            </Ellipse>
                            <TextBlock />

                        </StackPanel>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                            <faWPF:IconBlock Icon="Gear" Style="{StaticResource iconI}" Foreground="{StaticResource accentColor1}"  Width="20" Height="20" Margin="10,2" />
                            <TextBlock Text="Configuracion" 
                                         Foreground="{StaticResource fontColorSecondary}"
                                         FontFamily="Montserrat"
                                         FontSize="12"
                                         VerticalAlignment="Center"/>

                        </StackPanel>


                    </StackPanel>
                    <StackPanel Grid.Row="1" >

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Ellipse >
                            </Ellipse>
                            <TextBlock />

                        </StackPanel>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                            <CheckBox Margin="10" />
                            <TextBlock Text="Modo Oscuro/Modo Claro" 
                              Foreground="{StaticResource fontColorPrimary}"
                              FontFamily="Montserrat"
                              FontSize="12"
                              VerticalAlignment="Center"/>

                        </StackPanel>


                    </StackPanel>

                </Grid>


                <Border CornerRadius="0" 
                        Grid.Column="1">

                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.7">
                            <!--<GradientStop Color="{StaticResource secondaryBackColor1}" Offset="0.55" />-->
                            <GradientStop Color="{StaticResource secondaryBackColor2}" Offset="0" />

                        </LinearGradientBrush>
                    </Border.Background>



                </Border>


                <Border CornerRadius="0"
                        Grid.Column="0">

                    <Border.Background>
                        <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                            <GradientStop Color="{StaticResource primaryBackColor1}" Offset="0.55" />
                            <!--<GradientStop Color="{StaticResource primaryBackColor2}" Offset="0" />-->
                        </LinearGradientBrush>
                    </Border.Background>

                    <StackPanel>

                        <StackPanel Orientation="Horizontal"
                                Height="35"
                                Margin="15,25,0,40">

                            <faWPF:IconBlock Icon="Bug" FontSize="28" Height="30" Foreground="{StaticResource accentColor1}" />
                            <TextBlock Text="ScorePath"
                                   Foreground="{StaticResource fontColorPrimary}"
                                   FontSize="20"
                                   FontFamily="Montserrat"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"/>
                        </StackPanel>

                        <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource accentColor3}"
                                 IsChecked="False"
                                 Command="{Binding changepages}"
                                 CommandParameter="{x:Static models:ePageView.DashBoard}">

                            <StackPanel Orientation="Horizontal">
                                <faWPF:IconBlock Icon="Home" Style="{StaticResource menuButtonIcon}"/>
                                <TextBlock Text="Inicio" Style="{StaticResource menuButtonText}"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton 
                                 Style="{StaticResource menuButton}"
                                 Tag="{StaticResource accentColor4}"
                                 Command="{Binding changepages}"
                                 CommandParameter="{x:Static models:ePageView.Register}">
                            <StackPanel Orientation="Horizontal">
                                <faWPF:IconBlock Icon="Wpforms" Style="{StaticResource menuButtonIcon}"/>
                                <TextBlock Text="Registro" Style="{StaticResource menuButtonText}"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Style="{StaticResource menuButton}"
                                     Tag="{StaticResource accentColor5}"
                                     Command="{Binding changepages}"
                                     CommandParameter="{x:Static models:ePageView.Stats}">

                            <StackPanel Orientation="Horizontal">
                                <faWPF:IconBlock Icon="BarChart" Style="{StaticResource menuButtonIcon}"/>
                                <TextBlock Text="Estadisticas" Style="{StaticResource menuButtonText}"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Style="{StaticResource menuButton}"
                                     Tag="{StaticResource accentColor6}"
                                     Command="{Binding changepages}"
                                     CommandParameter="{x:Static models:ePageView.ViewChamps}">
                            <StackPanel Orientation="Horizontal">
                                <faWPF:IconBlock Icon="Table" Style="{StaticResource menuButtonIcon}"/>
                                <TextBlock Text="Tabla" Style="{StaticResource menuButtonText}"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Style="{StaticResource menuButton}"
                                     Tag="{StaticResource accentColor5}"
                                     Command="{Binding changepages}"
                                     CommandParameter="{x:Static models:ePageView.Graphic}">
                            <StackPanel Orientation="Horizontal">
                                <faWPF:IconBlock Icon="AreaChart" Style="{StaticResource menuButtonIcon}"/>
                                <TextBlock Text="Grafica" Style="{StaticResource menuButtonText}"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Style="{StaticResource menuButton}"
                                     Tag="{StaticResource accentColor8}"
                                     Command="{Binding changepages}"
                                     CommandParameter="{x:Static models:ePageView.GenerateDocument}">
                            <StackPanel Orientation="Horizontal">
                                <faWPF:IconBlock Icon="User" Style="{StaticResource menuButtonIcon}"/>
                                <TextBlock Text="Customers" Style="{StaticResource menuButtonText}"/>
                            </StackPanel>
                        </RadioButton>

                        <TextBlock Text="READY IK" Margin="30,0,30,-240" FontWeight="Regular" FontFamily="Montserrat"  Foreground="{StaticResource accentColor1}" VerticalAlignment="Bottom" />



                    </StackPanel>


                </Border>

                <Border CornerRadius="0,10,10,0"
                    Grid.Column="1">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                            <!--<GradientStop Color="{StaticResource primaryBackColor1}" Offset="0"/>-->
                            <GradientStop Color="{StaticResource primaryBackColor2}" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <!--Control bar-->
                            <RowDefinition Height="25"/>
                            <!--Header / Caption-->
                            <RowDefinition Height="45"/>
                            <!--Content section / Child views-->
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>


                        <!--Control bar-->
                        <StackPanel x:Name="pnlControlBar"
                                Grid.Row="0"
                                Orientation="Horizontal"
                                FlowDirection="RightToLeft"
                                Background="Transparent"
                                Margin="0,0,10,0"

                                
                                >
                            <Button x:Name="btnClose"
                                    
                                    Style="{StaticResource Control}"
                                    Tag="{StaticResource accentColor1}"                                    
                                    >
                                <faWPF:IconBlock Icon="Close" Style="{StaticResource ControlIcon}" />
                            </Button>

                            <Button x:Name="btnMaximize"
                                Style="{StaticResource Control}"
                                Tag="{StaticResource accentColor1}"
                                HorizontalContentAlignment="Center">



                                <faWPF:IconBlock Icon="WindowMaximize" Style="{StaticResource ControlIcon}" />

                            </Button>

                            <Button x:Name="btnMinimize"
                                    HorizontalContentAlignment="Right"
                                Style="{StaticResource Control}"
                                Tag="{StaticResource accentColor1}">

                                <faWPF:IconBlock Icon="WindowMinimize" Style="{StaticResource ControlIcon}" />
                            </Button>


                        </StackPanel>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <!--Caption-->
                            <StackPanel Grid.Column="0"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center">
                                <faWPF:IconBlock Icon="{Binding model.Icon}"
                                          Height="20"
                                          Width="20"
                                          Foreground="{StaticResource fontColorPrimary}"
                                          Margin="40,0,10,0"
                                          Name="IconPage"
                                                 FontSize="18"/>

                                <TextBlock Text="{Binding model.PageView}"
                                       Foreground="{StaticResource fontColorPrimary}"
                                       FontSize="16"
                                       FontFamily="Montserrat"
                                       FontWeight="Medium"
                                       VerticalAlignment="Center"
                                       Name="NamePage"/>
                            </StackPanel>

                            <!--User options-->
                            <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    FlowDirection="RightToLeft"
                                    VerticalAlignment="Center"
                                    Margin="0,0,10,0">
                                <Button Style="{StaticResource iconButton}"
                                        Tag="{StaticResource accentColor5}"
                                        >
                                    <faWPF:IconBlock Icon="Gear" Style="{StaticResource  iconI}"/>

                                </Button>

                                <Ellipse Height="35" Width="35"
                                     Stroke="{StaticResource accentColor2}"
                                     StrokeThickness="2"
                                     Margin="10,0,10,0">
                                    <!--<Ellipse.Fill>
                                        <ImageBrush ImageSource="C:\img\Coche Azul.jpeg"/>
                                    </Ellipse.Fill>-->
                                </Ellipse>
                                <TextBlock Text="Bienvenido invocador"
                                           Foreground="{StaticResource fontColorPrimary}"
                                           FontFamily="Montserrat"
                                           FontSize="12"
                                           VerticalAlignment="Center"/>
                                <Button Style="{StaticResource iconButton}"
                                    Tag="{StaticResource accentColor3}"
                                        
                                        Name="BT_Info">
                                    <faWPF:IconBlock Icon="Info" Style="{StaticResource  iconI}"/>
                                </Button>
                                <Button Style="{StaticResource iconButton}"
                                    Tag="{StaticResource accentColor4}">
                                    <faWPF:IconBlock Icon="Envelope" Style="{StaticResource iconI}"/>
                                </Button>

                            </StackPanel>

                        </Grid>

                        <Grid x:Name="item7" Grid.Row="2" Margin="25" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >



                        </Grid>



                    </Grid>
                </Border>
                <Border BorderThickness="2,2,0,0" Grid.Column="1" HorizontalAlignment="Right" Height="667" Name="B_Config" VerticalAlignment="Bottom" Width="240" CornerRadius="0,0,0,0" Visibility="Collapsed" >
                    <Border.BorderBrush>
                        <SolidColorBrush Color="{StaticResource winBorderColor2}" />
                    </Border.BorderBrush>
                    <Border.Background>
                        <SolidColorBrush Color="{StaticResource primaryBackColor1}" />
                    </Border.Background>

                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="25" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource iconButton}"
                                         Tag="{StaticResource accentColor6}"
                                         Command="{Binding bloqconfig}"
                                        >
                                    <faWPF:IconBlock Icon="Gear" Style="{StaticResource  iconI}"/>

                                </Button>

                                <TextBlock Text="Configuracion general" Foreground="{StaticResource fontColorPrimary}" Visibility="Hidden" />
                            </StackPanel>

                        </Grid>

                    </Grid>


                </Border>

            </Grid>



        </Border>
    </Border>
</Window>